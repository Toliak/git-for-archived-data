import * as path from 'path';
import * as fs from 'fs';

describe('Case MS Office Word file unpack and pack', () => {
    const targetDirectory = 'tests/e2e/wordFile';

    const configPath = path.join(targetDirectory);

    it('Full chain', () => {});
});
